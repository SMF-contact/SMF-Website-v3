import React from 'react';
import { NsJsonSchemaForm } from '@antv/xflow';
export interface IFormWrapper {
    children: (config: Object, plugin: {
        updateNode: (params: Object) => void;
        updateEdge: (params: Object) => void;
        updateGroup: (params: Object) => void;
    }) => React.ReactElement;
}
export declare const FormWrapper: React.FC<NsJsonSchemaForm.IControlProps & IFormWrapper & {
    type: string;
}>;

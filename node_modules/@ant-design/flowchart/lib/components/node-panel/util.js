"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createPath = exports.isNumber = exports.isType = void 0;
// 类型检测
var isType = function (value, type) {
    var toString = {}.toString;
    return toString.call(value) === "[object ".concat(type, "]");
};
exports.isType = isType;
var isNumber = function (value) {
    return (0, exports.isType)(value, 'Number');
};
exports.isNumber = isNumber;
// 创建节点路径
var createPath = function (paths, offsetX, offsetY) {
    if (offsetX === void 0) { offsetX = 0; }
    if (offsetY === void 0) { offsetY = 0; }
    if (!paths.length) {
        return null;
    }
    var path = '';
    // @ts-ignore
    paths.forEach(function (item) {
        var c = item[0], x = item[1], y = item[2], c2x = item[3], c2y = item[4];
        path += (0, exports.isNumber)(y) ? " ".concat(c, " ").concat(x + offsetX, " ").concat(y + offsetY) : " ".concat(c);
        if (c2y) {
            path += " ".concat(c2x + offsetX, " ").concat(c2y + offsetY);
        }
    });
    return path;
};
exports.createPath = createPath;

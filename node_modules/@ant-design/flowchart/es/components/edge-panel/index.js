var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import React from 'react';
import { LabelEdge } from './edges';
import { FLOWCHART_EDGE } from './constants';
export * from './constants';
export var registerEdge = function (config) {
    config.setEdgeRender(FLOWCHART_EDGE, function (props) { return React.createElement(LabelEdge, __assign({}, props)); });
};

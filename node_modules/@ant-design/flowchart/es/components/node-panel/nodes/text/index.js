import React, { useContext } from 'react';
import { AppContext } from '../../index';
import { NODE_WIDTH, NODE_HEIGHT } from '../../constants';
export var TextNode = function (props) {
    var _a = props.size, size = _a === void 0 ? { width: NODE_WIDTH, height: NODE_HEIGHT } : _a, _b = props.data, data = _b === void 0 ? {} : _b;
    var NodeConfig = useContext(AppContext).theme.NodeConfig;
    var stateNodeConfig = NodeConfig === null || NodeConfig === void 0 ? void 0 : NodeConfig.normal;
    var _c = data.label, label = _c === void 0 ? stateNodeConfig.label : _c, _d = data.fontFill, fontFill = _d === void 0 ? stateNodeConfig.fontFill : _d, _e = data.fontSize, fontSize = _e === void 0 ? stateNodeConfig.fontSize : _e;
    var width = size.width, height = size.height;
    // 用于放大文本，避免看不清
    var scale = 2;
    return (React.createElement("svg", { viewBox: "0 0 ".concat(width / scale, " ").concat(height / scale), xmlns: "http://www.w3.org/2000/svg", width: "100%", height: "100%" },
        React.createElement("text", { x: width / (2 * scale), y: height / (2 * scale), fill: fontFill, textAnchor: "middle", alignmentBaseline: "middle", fontSize: fontSize }, label),
        "Sorry, your browser does not support inline SVG."));
};

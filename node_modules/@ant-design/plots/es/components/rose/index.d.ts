import React from 'react';
import { RoseOptions as G2plotConfig } from '@antv/g2plot';
import { ChartRefConfig, ContainerConfig } from '../../interface';
export interface RoseConfig extends G2plotConfig, ContainerConfig<G2plotConfig> {
    chartRef?: ChartRefConfig;
}
declare const RoseChart: React.ForwardRefExoticComponent<RoseConfig & React.RefAttributes<unknown>>;
export default RoseChart;

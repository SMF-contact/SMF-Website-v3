import React from 'react';
import { ColumnOptions as G2plotConfig } from '@antv/g2plot';
import { ChartRefConfig, ContainerConfig } from '../../interface';
export interface ColumnConfig extends G2plotConfig, ContainerConfig<G2plotConfig> {
    chartRef?: ChartRefConfig;
}
declare const ColumnChart: React.ForwardRefExoticComponent<ColumnConfig & React.RefAttributes<unknown>>;
export default ColumnChart;

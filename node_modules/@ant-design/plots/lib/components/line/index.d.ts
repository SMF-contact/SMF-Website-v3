import React from 'react';
import { LineOptions as G2plotConfig } from '@antv/g2plot';
import { ChartRefConfig, ContainerConfig } from '../../interface';
export interface LineConfig extends G2plotConfig, ContainerConfig<G2plotConfig> {
    chartRef?: ChartRefConfig;
}
declare const LineChart: React.ForwardRefExoticComponent<LineConfig & React.RefAttributes<unknown>>;
export default LineChart;

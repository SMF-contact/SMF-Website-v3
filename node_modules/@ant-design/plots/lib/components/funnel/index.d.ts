import React from 'react';
import { FunnelOptions as G2plotConfig } from '@antv/g2plot';
import { ChartRefConfig, ContainerConfig } from '../../interface';
export interface FunnelConfig extends G2plotConfig, ContainerConfig<G2plotConfig> {
    chartRef?: ChartRefConfig;
}
declare const FunnelChart: React.ForwardRefExoticComponent<FunnelConfig & React.RefAttributes<unknown>>;
export default FunnelChart;

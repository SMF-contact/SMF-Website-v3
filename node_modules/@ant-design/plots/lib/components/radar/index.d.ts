import React from 'react';
import { RadarOptions as G2plotConfig } from '@antv/g2plot';
import { ChartRefConfig, ContainerConfig } from '../../interface';
export interface RadarConfig extends G2plotConfig, ContainerConfig<G2plotConfig> {
    chartRef?: ChartRefConfig;
}
declare const RadarChart: React.ForwardRefExoticComponent<RadarConfig & React.RefAttributes<unknown>>;
export default RadarChart;

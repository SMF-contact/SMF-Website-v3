import React from 'react';
import { GaugeOptions as G2plotConfig } from '@antv/g2plot';
import { ChartRefConfig, ContainerConfig } from '../../interface';
export interface GaugeConfig extends G2plotConfig, ContainerConfig<G2plotConfig> {
    chartRef?: ChartRefConfig;
}
declare const GaugeChart: React.ForwardRefExoticComponent<GaugeConfig & React.RefAttributes<unknown>>;
export default GaugeChart;

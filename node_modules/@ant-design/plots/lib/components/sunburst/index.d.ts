import React from 'react';
import { SunburstOptions as G2plotConfig } from '@antv/g2plot';
import { ChartRefConfig, ContainerConfig } from '../../interface';
export interface SunburstConfig extends G2plotConfig, ContainerConfig<G2plotConfig> {
    chartRef?: ChartRefConfig;
}
declare const SunburstChart: React.ForwardRefExoticComponent<SunburstConfig & React.RefAttributes<unknown>>;
export default SunburstChart;

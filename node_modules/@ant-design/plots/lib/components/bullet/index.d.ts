import React from 'react';
import { BulletOptions as G2plotConfig } from '@antv/g2plot';
import { ChartRefConfig, ContainerConfig } from '../../interface';
export interface BulletConfig extends Omit<G2plotConfig, 'color' | 'label' | 'style'>, ContainerConfig<G2plotConfig> {
    chartRef?: ChartRefConfig;
}
declare const BulletChart: React.ForwardRefExoticComponent<BulletConfig & React.RefAttributes<unknown>>;
export default BulletChart;

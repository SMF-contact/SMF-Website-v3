import React from 'react';
import { CommonConfig, NodeData } from '../../interface';
export declare type OrgItem = {
    name: string;
    title?: string;
    icon?: string;
};
export declare type OrganizationGraphData = NodeData<OrgItem[]>;
export interface OrganizationGraphConfig extends Omit<CommonConfig, 'data'> {
    data: OrganizationGraphData;
}
declare const OrganizationGraph: React.FC<OrganizationGraphConfig>;
export default OrganizationGraph;

import { LngLatExpression } from '../interface';
export declare class LngLat {
    lat: number;
    lng: number;
    alt?: number;
    constructor(lng: number, lat: number, alt?: number);
    equals(lnglat: LngLatExpression, threshold?: number): boolean;
    toString(precision: number): string;
    distanceTo(other: LngLatExpression): number;
    wrap(): LngLat;
    toBounds(sizeInMeters: number): import("./lng-lat-bounds").LngLatBounds;
    clone(): LngLat;
}
export declare function toLngLat(a: LngLatExpression | number, b?: number, c?: number): LngLat;

export interface IColorRamp {
    positions: number[];
    colors: string[];
}
export declare function isColor(str: any): boolean;
export declare function rgb2arr(str: string): number[];
export declare function decodePickingColor(color: Uint8Array): number;
export declare function encodePickingColor(featureIdx: number): [number, number, number];
export interface IImagedata {
    data: Uint8ClampedArray;
    width: number;
    height: number;
}
export declare function generateColorRamp(colorRamp: IColorRamp): ImageData | IImagedata;

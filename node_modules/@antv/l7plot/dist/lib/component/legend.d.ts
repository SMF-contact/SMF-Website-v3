import { IControlOption } from '@antv/l7-core';
import { Control } from '@antv/l7-component';
import { CategoryLegendOptions, ContinueLegendOptions } from '@antv/l7plot-component';
export declare type LegendType = 'category' | 'continue';
export declare type LegendItem = {
    type: LegendType;
    options: CategoryLegendOptions & ContinueLegendOptions;
};
export interface LegendOptions extends Partial<IControlOption> {
    items: LegendItem[];
}
export declare class Legend extends Control {
    /**
     * legend 的 schema 配置
     */
    protected options: LegendOptions;
    /**
     * legendComponents 实例
     */
    private legendComponents;
    constructor(options: LegendOptions);
    private initLegendComponents;
    /**
     * 获取默认配置
     */
    protected getDefaultOptions(): Partial<LegendOptions>;
    onAdd(): HTMLElement;
    onRemove(): void;
}

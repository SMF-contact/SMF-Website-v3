export default class TileCache {
    cache: any;
    constructor(limit: number | undefined, tileDestroy: any);
    getTile(key: string): any;
    setTile(tile: any, key: string): void;
    destory(): void;
    setNeedUpdate(): void;
}
